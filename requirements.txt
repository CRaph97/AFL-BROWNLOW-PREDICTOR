# Pinned per the Phase 1 technology decision: minimal dependencies, added only as needed.
# scikit-learn and scipy were added in Phase 3 (role-proxy decision tree; Spearman/point-biserial
# correlation) and are now also used in Phase 4 for HistGradientBoosting (Models 2a/2b), logistic
# regression (Model 0), isotonic calibration, and grouped cross-validation.
# XGBoost was attempted for Phase 4 but its macOS wheel could not load against this machine's
# installed libomp (a native-library ABI mismatch); scikit-learn's HistGradientBoosting* was used
# instead as a comparable histogram-based gradient boosting implementation -- see
# src/models/gbm_model.py. PyMC/statsmodels/lightgbm/catboost remain unused; add only if a future
# phase specifically needs them.

pandas==3.0.5
pyarrow==25.0.1        # parquet read/write
pyreadr==0.5.6         # reads fitzRoy's .rda files without needing R installed
requests==2.34.2
lxml==6.1.3            # HTML parsing (pandas.read_html backend), used for spot-check validation only
beautifulsoup4==4.15.0
html5lib==1.1
pytest==9.1.1
scipy==1.18.1          # Phase 3: Spearman / point-biserial correlation
scikit-learn==1.9.1    # Phase 3: role-proxy decision tree + grouped cross-validation + mutual information
