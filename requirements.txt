# Pinned per the Phase 1 technology decision: minimal dependencies, added only as needed.
# Full model-comparison libraries (xgboost/lightgbm/catboost, PyMC, statsmodels) are still NOT
# included -- Phase 3 is exploratory analysis / feature engineering only, not model fitting.
# scikit-learn and scipy were added in Phase 3, justified by a specific, bounded use each:
# scikit-learn for the role-proxy decision tree (an exploratory classifier, not a Brownlow
# predictor) and scipy for standard descriptive statistics (Spearman/point-biserial correlation).

pandas==3.0.5
pyarrow==25.0.1        # parquet read/write
pyreadr==0.5.6         # reads fitzRoy's .rda files without needing R installed
requests==2.34.2
lxml==6.1.3            # HTML parsing (pandas.read_html backend), used for spot-check validation only
beautifulsoup4==4.15.0
html5lib==1.1
pytest==9.1.1
scipy==1.18.1          # Phase 3: Spearman / point-biserial correlation
scikit-learn==1.9.1    # Phase 3: role-proxy decision tree + grouped cross-validation + mutual information
